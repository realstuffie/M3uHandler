{
  "name": "m3uhandler",
  "version": "1.0.0",
  "description": "Turn an IPTV-style M3U/M3U8 playlist into a media-server-friendly folder of .strm files.",
  "private": true,
  "scripts": {
    "daemon": "node src/daemon.js",
    "radarr-csv": "node src/radarr-csv.js",
    "radarr-adopt": "node src/radarr-adopt.js",
    "install-deps": "node src/install-deps.js",
    "lint:check": "node --check src/*.js tests/*.js",
    "test": "node --test"
  },
  "license": "GPL-3.0-or-later",
  "engines": {
    "node": ">=18"
  }
}
